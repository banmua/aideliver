import React from 'react';
import Button from '@material-ui/core/Button';

export default ({style}) => {
    return (
        <Button variant="contained" color="secondary" style={style}>Submit Your Order</Button>
    )
}